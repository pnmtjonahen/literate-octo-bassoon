management:
    endpoints:
        web:
            exposure:
                include: "*"

turbine:
    aggregator:
        clusterConfig: DINER
    appConfig: diner
    instanceUrlSuffix:
        default: actuator/hystrix.stream
