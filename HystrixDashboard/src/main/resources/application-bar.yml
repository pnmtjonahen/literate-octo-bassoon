management:
    endpoints:
        web:
            exposure:
                include: "*"

turbine:
    aggregator:
        clusterConfig: BARTENDER
    appConfig: bartender
    instanceUrlSuffix:
        default: actuator/hystrix.stream
