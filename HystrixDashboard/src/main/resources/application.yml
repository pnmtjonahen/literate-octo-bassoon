management:
    endpoints:
        web:
            exposure:
                include: "*"

turbine:
    aggregator:
        clusterConfig: DINER, BARTENDER
    appConfig: diner, bartender
    instanceUrlSuffix:
        default: actuator/hystrix.stream
