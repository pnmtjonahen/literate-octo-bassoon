management:
    endpoints:
        web:
            exposure:
                include: "*"

turbine:
    aggregator:
        clusterConfig: CHEF
    appConfig: chef
    instanceUrlSuffix:
        default: actuator/hystrix.stream
